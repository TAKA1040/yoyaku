'use client'

export const dynamic = 'force-dynamic'

import { useState } from 'react'
import Link from 'next/link'
import { Button } from '@/components/ui/button'
import { BookingForm } from '@/components/BookingForm'
import { ArrowLeft, CheckCircle } from 'lucide-react'

export default function BookingPage() {
  const [bookingCompleted, setBookingCompleted] = useState(false)
  const [completedBookingId, setCompletedBookingId] = useState<string>('')

  const handleBookingSuccess = (bookingId: string) => {
    setCompletedBookingId(bookingId)
    setBookingCompleted(true)
  }

  return (
    <div className="min-h-screen bg-gray-50">
      {/* ヘッダー */}
      <header className="bg-white shadow-sm border-b">
        <div className="container mx-auto px-4 py-4 flex items-center justify-between">
          <div className="flex items-center space-x-4">
            <Link href="/">
              <Button variant="ghost" size="sm">
                <ArrowLeft className="h-4 w-4 mr-2" />
                トップページ
              </Button>
            </Link>
            <h1 className="text-xl font-semibold text-gray-900">
              オンライン予約
            </h1>
          </div>
          <div className="text-sm text-gray-600">
            {process.env.NEXT_PUBLIC_APP_NAME || 'クリニック'}
          </div>
        </div>
      </header>

      {/* メインコンテンツ */}
      <main className="container mx-auto px-4 py-8">
        {!bookingCompleted ? (
          <BookingForm onSuccess={handleBookingSuccess} />
        ) : (
          <BookingSuccessMessage bookingId={completedBookingId} />
        )}
      </main>

      {/* フッター */}
      <footer className="bg-white border-t mt-16">
        <div className="container mx-auto px-4 py-6">
          <div className="text-center text-sm text-gray-600">
            <p className="mb-2">
              ご不明な点がございましたら、お気軽にお問い合わせください。
            </p>
            <div className="flex justify-center space-x-6">
              <span>📞 TEL: 03-1234-5678</span>
              <span>📧 EMAIL: info@clinic.example.com</span>
            </div>
          </div>
        </div>
      </footer>
    </div>
  )
}

function BookingSuccessMessage({ bookingId }: { bookingId: string }) {
  return (
    <div className="max-w-2xl mx-auto text-center">
      <div className="bg-white rounded-lg shadow-lg p-8">
        <div className="w-16 h-16 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-6">
          <CheckCircle className="h-10 w-10 text-green-600" />
        </div>
        
        <h1 className="text-2xl font-bold text-gray-900 mb-4">
          ご予約ありがとうございます！
        </h1>
        
        <div className="space-y-4 text-left bg-gray-50 rounded-lg p-6 mb-6">
          <div className="flex justify-between">
            <span className="font-medium">予約番号:</span>
            <span className="font-mono text-sm">{bookingId}</span>
          </div>
        </div>
        
        <div className="space-y-4 text-sm text-gray-600">
          <div className="flex items-start space-x-3">
            <span className="text-green-600 font-bold">✓</span>
            <div>
              <p className="font-medium">確認メールを送信しました</p>
              <p>予約内容とカレンダーファイル（ICS）が添付されています。</p>
            </div>
          </div>
          
          <div className="flex items-start space-x-3">
            <span className="text-green-600 font-bold">✓</span>
            <div>
              <p className="font-medium">前日リマインダーをお送りします</p>
              <p>予約の前日に、メールまたはSMSでお知らせいたします。</p>
            </div>
          </div>
          
          <div className="flex items-start space-x-3">
            <span className="text-blue-600 font-bold">📋</span>
            <div>
              <p className="font-medium">予約の変更・キャンセル</p>
              <p>確認メール内のリンクから、いつでも変更・キャンセルが可能です。</p>
            </div>
          </div>
        </div>
        
        <div className="mt-8 space-y-4">
          <div className="bg-yellow-50 border border-yellow-200 rounded-lg p-4">
            <p className="text-sm text-yellow-800">
              <span className="font-medium">当日のご案内</span><br />
              予約時間の10分前にお越しください。
            </p>
          </div>
          
          <div className="flex flex-col sm:flex-row gap-3">
            <Link href="/" className="flex-1">
              <Button variant="outline" className="w-full">
                トップページに戻る
              </Button>
            </Link>
            <Link href="/booking" className="flex-1">
              <Button 
                className="w-full"
                onClick={() => window.location.reload()}
              >
                別の予約を取る
              </Button>
            </Link>
          </div>
        </div>
      </div>
    </div>
  )
}